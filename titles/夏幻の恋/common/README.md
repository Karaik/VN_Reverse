# 公共层

这里存放 `script/` 与 `archive/` 共同复用的正式公共逻辑。

本层适合放：

- 二进制读写辅助
- 共享常量与数据模型
- 路径、编码、清单等公共工具
- 根入口需要复用的轻量适配逻辑

本层不放：

- 只服务单一脚本格式的专用实现
- 只服务单一封包格式的专用实现
- `IDA` 分析资料或实验脚本

## 当前状态

- 当前正式执行链没有运行时依赖 `tmp/legacy_20260326_snapshot/`
- `legacy_shim.py` 只保留为迁移早期留下的历史辅助文件
- 它不是当前默认用法的一部分
