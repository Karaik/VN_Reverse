# 验证

## 已验证

- `update1.ypf` 中的 `ysbin/*` 可以解出
- `YSTL_Parse.exe -make` 可以还原脚本路径
- `yst00211.ybn` 对应 `data/script/userscript/テキスト/main01a.txt`
- 三行文本导出可以跑通
- `galtrans_json` 导出可以跑通
- `キャラ名定義.txt` 中的人名可以抽取
- `UQB2S_chs.exe` 可以构建
- 真实输出目录中的 `UQB2S_chs.exe` 可以直接从相对路径启动
- `patch_chs.log` 可以记录真实路径启动期的文件访问与路径修正日志
- 只改 `main01a` 开头一小段真实中文后，重新打进 `update1.ypf` 仍可进入正文，不再触发调试异常

## 当前风险

- `translated_script/galtrans_json -> translated_script/triline` 这条链已经固定，但仍需继续做全量回归
- `name_define` 与正文前缀的匹配链仍在核查，当前仍需继续验证“名字进入名字框”
- 当前发布形态是 `patch_chs/ + UQB2S_chs.exe + patch_chs.dll`
