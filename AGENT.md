# VN_Reverse AGENT 规则

## 作用范围

- 本文件对整个 `VN_Reverse` 仓库生效。
- 子目录内的 `AGENT.md` 可以补充细节，但不得弱化本文件规则。

## 项目概况

- `titles/`：按具体游戏划分的逆向工程工作区（格式文档、工具、测试）。
- `engines/`：引擎层通用工具与查看器。
- `skills/`：流程与能力扩展支持文件。

本仓库以工程产出为中心，要求结果可执行、可验证、表达简洁。

## 强规则（必须遵守）

1. 文档中禁止添加无信息量的“阅读提示”或“渲染提示”。
2. 以下内容视为禁止项：
   - 在标题中附加编辑器兼容性后缀。
   - 任何“可被某编辑器/工具渲染”这一类说明文字。
   - 其他不增加技术信息的兼容性提示语。
3. 标题必须直接表达内容本身。
   - 使用 `## （标题）`，不要附加说明。
4. 文档内容必须与任务直接相关，包括但不限于：
   - 格式定义
   - 字段语义
   - 算法细节
   - 命令用法
   - 测试与验证结果
5. 任何不服务于实现、验证、维护的信息都应删除。
6. 反编译和编译必须能处理脚本中的文本，否则视为失败。
7. 脚本逆向必须同时提供两种可用形态：  
   - 指令集形态（可编辑、可回编）  
   - JSON 形态（可编辑、可回编）
8. 脚本编译不得依赖“原长度不变”假设。凡是指令中包含长度字段、偏移表、索引表的格式，编译时必须重算并回写。
9. 回归测试必须包含“文本长度变化”用例（变长/缩短至少一种），验证编译产物可再次反编译且文本一致；仅做字节原样回环不够。

## 写作标准

- 优先使用简洁、确定性的表达。
- 避免营销化、解释性废话。
- 示例必须可直接执行，路径必须明确。
