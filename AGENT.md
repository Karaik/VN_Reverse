# VN_Reverse AGENT 规则

## 作用范围

- 本文件对整个 `VN_Reverse` 仓库生效。
- 子目录内的 `AGENT.md` 可以补充细节，但不得弱化本文件规则。

## 项目概况

- `titles/`：按具体游戏划分的逆向工程工作区（格式文档、工具、测试）。
- `engines/`：引擎层通用工具与查看器。
- `skills/`：流程与能力扩展支持文件。

本仓库以工程产出为中心，要求结果可执行、可验证、表达简洁。

## 强规则（必须遵守）

1. 文档中禁止添加无信息量的“阅读提示”或“渲染提示”。
2. 以下内容视为禁止项：
   - 在标题中附加编辑器兼容性后缀。
   - 任何“可被某编辑器/工具渲染”这一类说明文字。
   - 其他不增加技术信息的兼容性提示语。
3. 标题必须直接表达内容本身。
   - 使用 `## （标题）`，不要附加说明。
4. 文档内容必须与任务直接相关，包括但不限于：
   - 格式定义
   - 字段语义
   - 算法细节
   - 命令用法
   - 测试与验证结果
5. 任何不服务于实现、验证、维护的信息都应删除。
6. 反编译和编译必须能处理脚本中的文本，否则视为失败。
7. 脚本逆向必须同时提供两种可用形态：  
   - 指令集形态（可编辑、可回编）  
   - JSON 形态（可编辑、可回编）
8. 脚本编译不得依赖“原长度不变”假设。凡是指令中包含长度字段、偏移表、索引表的格式，编译时必须重算并回写。
9. 回归测试必须包含“文本长度变化”用例（变长/缩短至少一种），验证编译产物可再次反编译且文本一致；仅做字节原样回环不够。
10. 若源脚本文本编码为 `win-31j/sjis/cp932` 这类多字节编码，必须提供“回写指定编码”参数（如 `--text-encoding gbk`），并在 usage 给出 `gbk` 示例；`u16/u8` 直写格式不要求此参数。
11. 若脚本文本中混有控制符/指令片段，必须支持基于同级 `filter_text.txt` 的过滤回写：命中过滤项时强制按源编码回写，不使用目标编码。

## 写作标准

- 优先使用简洁、确定性的表达。
- 避免营销化、解释性废话。
- 示例必须可直接执行，路径必须明确。

## 标准流程（新 title 逆向）

1. 建立工作区  
   - 在 `titles/<游戏名>/` 下建立独立工作目录。  
   - 明确引擎名并写入该 title 的 `README.md` 标题。  
2. 先判格式再写工具  
   - 先从可执行文件反编译代码与样本文件入手，确认脚本格式与封包格式。  
   - 在未确认字段语义前，不得先写“猜测型”编译器。  
3. 工具最小交付集合  
   - 脚本：反编译工具、编译工具（默认 JSON 路径）。  
   - 脚本：指令集文本导出与回编路径（可编辑、可回编）。  
   - 资源：解包工具、封包工具。  
   - 工具需支持单文件与目录递归两种输入。  
4. 文本汉化可用性要求  
   - 反编译结果必须可直接修改文本后回编。  
   - 对长度字段、偏移表、索引表必须重算并回写。  
   - 文本变长后不得造成运行时读取错位或崩溃。  
   - 对 `win-31j/sjis/cp932` 脚本必须提供编码回写参数，并在文档提供 `gbk` usage 示例（`u16/u8` 直写格式除外）。  
   - 对文本内控制符场景必须提供 `filter_text.txt` 过滤回写机制，并覆盖回归测试。  
5. 文档交付要求  
   - 至少提供脚本结构文档与封包结构文档。  
   - 文档必须写清字段、偏移、算法、命令、限制、回归结果。  
   - Mermaid 图只保留技术结构信息，不写渲染提示语。  
6. 回归测试要求  
   - 必做：脚本原样回环（反编译->编译==原文件）。  
   - 必做：指令集路径回环（指令集导出->回编==原文件）。  
   - 必做：文本变长回归（改文本后可反编译且文本一致）。  
   - 必做：封包回环（解包->封包==原文件）。  
7. README 与索引更新  
   - 更新该 title 的 `README.md`（工具作用表、文档入口、usage）。  
   - 更新仓库根 `README.md` 的 titles 表格（引擎与游戏名分列，链接内部 README）。  
8. 提交规范  
   - 仅提交本次任务相关文件。  
   - 用户要求 amend 时，必须执行 `commit --amend` 并同步远程。  
