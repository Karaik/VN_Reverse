# VN_Reverse AGENT 规则

## 作用范围

- 本文件对整个 `VN_Reverse` 仓库生效。
- 子目录内的 `AGENT.md` 可以补充项目本地细节，但不得弱化本文件的仓库级规则。

## 仓库定位

这个仓库不是单一类型项目仓库，而是一个混合入口仓库，主要包含三类内容：

- `titles/`：按具体游戏或项目划分的工作目录，内部可能是汉化项目、逆向项目，或仍在占位阶段的条目。
- `engines/`：引擎层工具、查看器、解析器等独立工程。
- `skills/`：提供给代码代理使用的流程型技能与能力型技能。

本仓库的首要目标是：让人和 AI 都能先识别项目类型，再进入对应规则，而不是默认把所有 `title` 当成同一种项目。

## 项目类型判定

仓库级正式项目类型只使用两类：

- 汉化项目
- 逆向项目

处理 `titles/<name>/` 时，先判断它最终应归到哪一类：

### 汉化项目

常见信号：

- 存在 `main_build.py`
- 存在 `game_script/translated_script/`
- 存在 `solution/patch/`、`solution/runtime/`、`solution/build/`
- README 关注补丁、构建、冒烟、发布包、增量更新

进入这类项目后，优先遵循：

- `docs/project-types/localization.md`
- 该 title 自己的 `README.md`
- 该 title 内更具体的 `AGENT.md`（如果存在）

### 逆向项目

常见信号：

- 目录内以 `*_decompile.py`、`*_compile.py`、`*_pack.py`、`*_unpack.py`、`regression_test.py` 为主
- 存在 `docs/*_结构.md`
- README 关注格式、字段、回环、验证、指令源码
- 不以最终补丁包或可运行汉化程序为主要交付物

进入这类项目后，优先遵循：

- `docs/project-types/reverse.md`
- 该 title 自己的 `README.md`
- 该 title 内更具体的 `AGENT.md`（如果存在）

### 尚未归类的目录

如果某个目录还只有极少量文件、只有入口 README，或暂时无法确认最终应归到哪一类：

- 先记录为待判定目录
- 不要把“待判定”当成第三种正式项目类型
- 等目标和执行链明确后，再归入汉化项目或逆向项目

## 仓库级规则

1. 先判项目类型，再决定工作流；禁止把“汉化项目规则”直接套到“逆向项目”，也禁止反过来套用。
2. 技术文档必须直接服务于实现、验证、维护；避免把对 AI 的长期记忆、个人备注、展示文案混进仓库级规范。
3. 面向人的总览信息放 `README.md`；面向代理的执行规则放 `AGENT.md`；格式、流程、结构细节放 `docs/` 或项目内文档。
4. 仓库级文档只写稳定规则和路由信息，不写某一个 title 的局部习惯。
5. 所有命令示例优先使用相对路径，并以对应项目根目录为基准。
6. 不要把临时目录、缓存目录、下载目录写成默认依赖路径。
7. 如果项目已有更具体的本地规则，优先跟随本地规则；如果本地规则与仓库级规则冲突，先停下来做范围确认。
8. 准备进入主库时，按实际需要决定是否起 issue、开分支、提 PR；协作边界见 `docs/workflows/git-issue-pr.md`。

## 技术文档规则

- 标题直接表达内容，不加渲染提示和兼容性废话。
- 示例必须可执行，路径必须明确。
- 结构文档、用法文档、验证结论分开写，避免混成一个“大杂烩 README”。
- 不要把“主要给 AI 和我自己看的”内容继续塞进公开 README；这类内容应迁移到 `AGENT.md` 或专门的内部文档。
- 仓库内自写文档的自然语言必须保持单语一致；默认使用中文，不要在自然语言层面中英混写。
- 文件路径、命令、代码标识、格式名、项目名、上游仓库名等技术标识可保留原文。
- 外部来源文档或上游保留文档不受这条限制，但应在本仓库说明文档里写清来源与改写状态。

## skills 规则

- `skills/` 只存放技能自身，不承担 title 项目的长期状态记录。
- 技能分两类：
  - 流程型技能：定义一整类项目的工作流、目录模板、交付边界。
  - 能力型技能：完成某个局部分析任务，例如符号恢复、结构恢复。
- 目录布局与命名约定见 `docs/skills-layout.md` 与 `skills/README.md`。

## 文档入口

- 仓库总览：`README.md`
- 文档索引：`docs/README.md`
- 汉化项目规则：`docs/project-types/localization.md`
- 逆向项目规则：`docs/project-types/reverse.md`
- 协作约定：`docs/workflows/git-issue-pr.md`
- 困难脚本转向策略：`docs/workflows/hard-script-pivot.md`
- skills 目录规范：`docs/skills-layout.md`
- 技能总览：`skills/README.md`
